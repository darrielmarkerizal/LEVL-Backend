includes:
    - ./vendor/nunomaduro/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    # Level scan paling ketat
    level: max

    # Folder yang dianalisis (kode utama)
    paths:
        - app
        - Modules
        - bootstrap
        - routes
        - resources

    # Folder yang dikecualikan (non-kode atau vendor)
    excludePaths:
        - */vendor/*
        - */storage/*
        - */node_modules/*

    # Autoload file Laravel supaya container & service provider dikenali
    autoload_files:
        - bootstrap/app.php

    # Abaikan error pada ignoreErrors yang tidak ada
    reportUnmatchedIgnoredErrors: false
