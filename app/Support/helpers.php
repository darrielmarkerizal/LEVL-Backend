<?php

/**
 * Helper functions for the application.
 *
 * Note: File upload helpers have been removed as file handling
 * is now done via Spatie Media Library directly on models.
 *
 * Usage example:
 *   $model->addMedia($file)->toMediaCollection('collection-name');
 *   $url = $model->getFirstMediaUrl('collection-name');
 */
